apiVersion: v1
kind: Service
metadata:
  name: quickcart-api
  namespace: quickcart

spec:
  selector:
    app: quickcart-api

  ports:
    - name: http
      port: 8000
      targetPort: 8000

  type: ClusterIP