apiVersion: v1
kind: Service
metadata:
  name: quickcart-web
  namespace: quickcart

spec:
  selector:
    app: quickcart-web

  ports:
    - name: http
      port: 80
      targetPort: 80

  type: LoadBalancer